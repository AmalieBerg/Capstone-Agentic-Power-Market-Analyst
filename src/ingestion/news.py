"""News inlet (U1.3, Sprint 2): query-scoped Google News RSS + fixed energy
feeds + Guardian API. Store metadata only (title/URL/source/time) + embeddings (D4).
"""
from __future__ import annotations

# TODO U1.3: query-scoped RSS via feedparser; Guardian full text via GUARDIAN_API_KEY.
